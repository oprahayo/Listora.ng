<x-layouts.public>
    @section('title', $agent->display_name.' | Listora.ng')
    <section class="mx-auto max-w-[1180px] px-4 py-8 sm:px-6 lg:px-0"><div class="rounded-xl border border-[#D7E2F4] bg-white p-5"><div class="flex flex-wrap items-start gap-4"><span class="flex size-14 items-center justify-center rounded-full bg-[#0A2856] text-lg font-semibold text-white">{{ Str::of($agent->display_name)->explode(' ')->map(fn($part)=>Str::substr($part,0,1))->take(2)->join('') }}</span><div><div class="flex flex-wrap items-center gap-2"><h1 class="text-[27px] font-semibold text-[#182230]">{{ $agent->display_name }}</h1>@if($agent->isVerified())<x-status-badge type="verified" icon="shield-check">Verified</x-status-badge>@endif</div><p class="mt-1 text-sm text-[#667085]">{{ collect([$agent->operating_city,$agent->operating_state])->filter()->join(', ') }}</p>@if($agent->short_bio)<p class="mt-3 max-w-2xl text-sm leading-6 text-[#475467]">{{ $agent->short_bio }}</p>@endif</div></div></div>
        <div class="mt-6 flex items-center justify-between"><h2 class="text-xl font-semibold text-[#182230]">Listed properties</h2><a href="{{ route('properties.index',['q'=>$agent->display_name]) }}" class="text-sm font-medium text-[#145FCC]">See all</a></div><div class="mt-3 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">@forelse($agent->properties as $property)<x-property-card :property="$property" />@empty<x-empty-state icon="building" title="No listed properties" message="Check again later for new listings." />@endforelse</div>
    </section>
</x-layouts.public>
