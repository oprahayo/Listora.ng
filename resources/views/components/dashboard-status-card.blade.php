@props(['icon' => 'info', 'title', 'status' => null, 'message', 'action' => null, 'href' => null])
<section {{ $attributes->merge(['class' => 'rounded-xl border border-[#D7E2F4] bg-white p-4']) }}><div class="flex items-start gap-3"><span class="flex size-10 shrink-0 items-center justify-center rounded-lg bg-[#EAF2FF] text-[#145FCC]"><x-icon :name="$icon" /></span><div class="min-w-0 flex-1"><div class="flex flex-wrap items-center gap-2"><h2 class="text-[16px] font-semibold">{{ $title }}</h2>@if($status)<x-status-badge>{{ $status }}</x-status-badge>@endif</div><p class="mt-1 text-sm leading-5 text-[#667085]">{{ $message }}</p></div></div>@if($action && $href)<a href="{{ $href }}" class="btn-primary mt-4 w-full sm:w-auto">{{ $action }}</a>@endif</section>
