<x-layouts.public>
    @section('title', 'Landlord dashboard | Listora.ng')
    <section class="dashboard-shell"><div class="flex flex-wrap justify-between gap-4"><div><p class="text-sm text-[#667085]">Landlord workspace</p><h1 class="text-[27px] font-semibold text-[#182230]">Hello, {{ auth()->user()->name }}</h1></div>@if(auth()->user()->roles()->count()>1)<a href="{{ route('workspace.index') }}" class="btn-secondary">Switch workspace</a>@endif</div>
        <div class="mt-5 rounded-xl border border-[#D7E2F4] bg-white p-4"><div class="flex items-center justify-between gap-3"><div><h2 class="text-lg font-semibold text-[#182230]">Account status</h2><p class="mt-1 text-sm text-[#667085]">Contact preference: {{ str($profile?->preferred_contact_method ?: 'app')->headline() }}</p></div><x-status-badge type="verified">Basic</x-status-badge></div></div>
        <div class="mt-6 grid grid-cols-2 gap-3 md:grid-cols-4"><x-icon-card icon="users" title="Invitations" :subtitle="$invitations->count().' recent'" :href="route('notifications.index')"/><x-icon-card icon="building" title="Agents" :href="route('notifications.index')"/><x-icon-card icon="user" title="Profile" :href="route('onboarding.landlord')"/><x-icon-card icon="settings" title="Settings" :href="route('onboarding.landlord', ['step'=>2])"/></div>
        @if($invitations->isNotEmpty())<div class="mt-6 rounded-xl border border-[#E4E7EC] bg-white p-4"><h2 class="text-lg font-semibold">Connected agent invitations</h2>@foreach($invitations as $invitation)<div class="mt-3 flex items-center justify-between border-t border-[#E4E7EC] pt-3 text-sm"><span>{{ $invitation->inviter?->name ?: 'Listora agent' }}</span><x-status-badge>{{ str($invitation->status)->headline() }}</x-status-badge></div>@endforeach</div>@endif
    </section>
</x-layouts.public>
